# 모델 단가표. 단위: USD / 1M 토큰.
#
#   - 여기에 없는 모델은 비용 0 으로 계상하고 경고만 냅니다. 절대 죽지 않습니다.
#   - 무료 모델은 0 으로 명시하세요. 그러면 "가격 미상" 경고가 안 뜹니다.
#     (0 을 적는 것 = "공짜인 걸 안다", 생략 = "모른다" — 둘은 다릅니다)
#   - 키는 프로바이더가 반환하는 모델 ID 그대로 씁니다.
#
# 실제 모델 ID 가 확정되면 아래에 채우세요. 형식:
#
#   gpt-4o-mini:
#     input_per_1m: 0.15
#     output_per_1m: 0.60

models:
  # fake 모드 전용. 슬라이스 1 검증에 사용.
  fake-a:
    input_per_1m: 0.0
    output_per_1m: 0.0
  fake-b:
    input_per_1m: 0.0
    output_per_1m: 0.0
